# Attendance synchronization

The production application includes an automated attendance synchronization service. That service and its credentials are intentionally omitted from this portfolio repository.

The portfolio uses fictional records from the browser-local data adapter. It must not be configured with production scheduling, attendance, website API, or Firebase credentials.
